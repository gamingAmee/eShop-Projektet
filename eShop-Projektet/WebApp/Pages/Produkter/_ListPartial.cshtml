@using ServiceLayer.ProduktService;
@model ProduktListDto

    <div class="col-sm-3">
        <div class="card my-3">
            <img class="card-img-top" src="@Model.Billede">
            <div class="card-body">
                <h5 class="card-title">@Model.Navn</h5>
                <p class="card-text">@Model.Pris</p>
            </div>
            @*<div class="card-footer text-muted">
                <a asp-page="./Detail" asp-route-restaurantId="@Model.ProduktId">
                    <i class="fas fa-info-circle"></i>
                </a>
                <a asp-page="./Edit" asp-route-restaurantId="@Model.ProduktId">
                    <i class="fas fa-edit"></i>
                </a>
                <a asp-page="./Delete" asp-route-restaurantId="@Model.ProduktId">
                    <i class="fas fa-trash"></i>
                </a>
            </div>*@
        </div>
    </div>
